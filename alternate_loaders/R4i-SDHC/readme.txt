This boots `_BOOT_DS.NDS` in root. `_BOOT_DS.NDS` must be DLDI patched with `TTCARDIOLibrary.dldi` found in `/BootStrap/DLDI`.

credits: fincs for the loader and source. Still can't figure out how to build my own package on latest libnds but eh